---
ServerDelete:
  implicit:
    _memory:
      type: string
    _disk:
      type: string
  providers:
    - action: delete
      match:
        type: object
        properties:
          providerType:
            type: string
      parameters:
        resource_id:
          required: true
          explicit:
            type: string
      debug: true