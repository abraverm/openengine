---
ServerDelete:
  implicit:
    _memory:
      type: string
    _disk:
      type: string
  providers:
    - action: delete
      parameters:
        resource_id:
          required: true
          explicit:
            type: string